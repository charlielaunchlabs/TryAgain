using System;
using System.Collections.ObjectModel;
using Xamarin.Forms;

namespace TryAgain
{
	public class ListLugar : StackLayout
	{


		public ListLugar()
		{
			//lista = new ListView();
			//lista.ItemTemplate = new DataTemplate(typeof(CustomListChuchu));
			//var all = App.DAUtil.AllUser();

			//var oc = new ObservableCollection<Lugars>(all);
			//lista.ItemsSource = oc;
			//lista.BindingContext = oc;
			//lista.SeparatorVisibility = Xamarin.Forms.SeparatorVisibility.None;
			//lista.ItemTapped += async (sender, e) =>
			//{
			//	Lugars item = (Lugars)e.Item;
			//	//navigate to nxt map
			//	await Navigation.PushModalAsync(new NavigationPage(new SelectedMap(item)));
			//};
			//this.Children.Add(lista);
		}
	}


}


